# caddy-cloudflare
Caddy package with cloudflare plugin

Published images include a `caddy-<version>` tag (for example `caddy-2.10`) and the `org.opencontainers.image.base.name` label so the upstream Caddy version is easy to identify.
